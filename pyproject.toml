[project]
name = "simplecutpy"
version = "0.3.0"
description = "A simple video cut tool based on wxpython, ffmpeg"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "logging>=0.4.9.6",
    "pymediainfo>=7.0.1",
    "pypubsub>=4.0.3",
    "wxpython>=4.2.2",
]
